#Esta vcariable contiene una lista con los argumentos pasados a una función o a un script en su ejecución
$args

#Esta variable indica siu la última instrucción se ejecutó correctamente o no
$?

#Esta variable idica el mprimer miembro del último comando tecleado en la linea de comandos
$^

#Esta variable indica el último miembro del ´ltimo comando tecleado en la línea de comandos
$$

#Esta variable registra lso errores producidos durante la ejecución de la sesión
$Error

#variable que indica el formato en el que se mostrarán lso errores registrados en $Error
$ErrorView

#Variabe que almacena la ruta del directorio de inicio del usuario
$HOME

#Variable que contiene el identificador del proces dep PowerShell
$PID